name: Verify candidate promotion plan

on:
  pull_request:
    paths:
      - "contracts/candidate-review-v0.schema.json"
      - "contracts/candidate-promotion-plan-v0.schema.json"
      - "docs/adr/0013-builder-candidate-promotion-plan.md"
      - "tools/review_builder_candidate.py"
      - "tools/plan_builder_candidate_promotion.py"
      - "tests/candidate_promotion_plan_test.py"
      - ".github/workflows/verify-candidate-promotion-plan.yml"
  push:
    branches:
      - main
    paths:
      - "contracts/candidate-review-v0.schema.json"
      - "contracts/candidate-promotion-plan-v0.schema.json"
      - "docs/adr/0013-builder-candidate-promotion-plan.md"
      - "tools/review_builder_candidate.py"
      - "tools/plan_builder_candidate_promotion.py"
      - "tests/candidate_promotion_plan_test.py"
      - ".github/workflows/verify-candidate-promotion-plan.yml"

permissions:
  contents: read

concurrency:
  group: verify-candidate-promotion-plan-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pinned JSON Schema validator
        run: python -m pip install --disable-pip-version-check "jsonschema==4.23.0"

      - name: Verify additive promotion planning contract
        run: |
          python -m py_compile \
            tools/review_builder_candidate.py \
            tools/plan_builder_candidate_promotion.py \
            tests/candidate_promotion_plan_test.py
          python tests/candidate_promotion_plan_test.py
