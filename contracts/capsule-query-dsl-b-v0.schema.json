{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://logiclens.local/contracts/capsule-query-dsl-b-v0.schema.json",
  "title": "LogicLens DSL-B capsule query v0",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "dslLevel", "operation", "target"],
  "properties": {
    "schemaVersion": {"const": "0.1"},
    "dslLevel": {"const": "DSL-B"},
    "operation": {"const": "derived-strict-claim"},
    "target": {
      "type": "object",
      "additionalProperties": false,
      "required": ["predicate", "arguments"],
      "properties": {
        "predicate": {
          "type": "string",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"
        },
        "arguments": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": {"type": ["string", "integer", "boolean"]}
        }
      }
    }
  }
}
