# WP-003 related-work search log — Q15 addendum

Date: 2026-08-11
Producer role: Novelty Reviewer remediation session
Independent acceptance: **not performed**
HOLDOUT/REPLICATION access: **none**

## Trigger

Independent R3 returned `REVISE (freshness/saturation only)` because Q13/Q14 omitted materially relevant 2026 work around contract-grounded verified execution, typed executable operation languages, frozen-small-model routing and authoritative-state gating.

## Primary-source indexes

- ACL Anthology
- arXiv primary abstract page

Search snippets were used only for retrieval. Included scientific descriptions were checked against the primary ACL Anthology/arXiv records.

## Q15 frozen query family

1. `contract grounded verified tool execution typed trusted symbolic state LLM`
2. `schema contract typed operation deterministic SQL LLM`
3. `frozen small language model routing probe agent`
4. `small language model base tool agent routing comparison`
5. `explicit state conflicting rules guideline following LLM`
6. `deterministic executability gating authoritative state skill selection LLM`

Retrieval-count definition: unique primary candidates actually opened and dispositioned, not unstable search-engine hit counts.

## Screened primary candidates

- `C-059 / RW-059` — ToolGate — include_nearest
- `C-060 / RW-060` — Text2Mem — include_nearest
- `C-061 / RW-061` — LatentGate — include_control
- `C-062 / RW-062` — Rethinking Scale — include_control
- `C-063 / RW-063` — Guidelines as Environments — include_control
- `C-064 / RW-064` — Don't Offer What Can't Be Done — include_nearest

All six are included because each materially occupies a mechanism or causal alternative relevant to ENG-197/199/200 or the core trusted-interface interpretation. None is excluded merely because it weakens novelty.

## Frozen seven-dimension threshold

Dimensions remain unchanged from Q11-Q14:

1. fixed-weight small/open student;
2. matched interface contrast;
3. trusted deterministic semantics;
4. four-state epistemic status;
5. verified-result renderer;
6. structure/no-conclusion/copying controls;
7. independent layer oracle/scorer.

Threshold: at least five strict `YES`; `PARTIAL` does not count.

Result: **0 / 6 candidates reach five strict YES dimensions**.

This is coverage evidence only. It is not evidence of priority and cannot support `first`, `unique`, or `unprecedented` wording.

## Freshness decision

- Q11/Q12: reproducible historical rounds, superseded by transfer-ladder expansion.
- Q13/Q14: reproducible transfer-ladder refresh, superseded as *latest* freshness evidence by independent R3 omissions.
- **Q15: latest usable producer-side W0 freshness round.**

## Mandatory manuscript/control consequences

The current novelty boundary must explicitly treat as occupied:

- typed trusted symbolic state + runtime-verified tool state evolution (ToolGate);
- schema-based semantic contracts → typed operations → deterministic SQL/reference execution (Text2Mem);
- frozen-SLM routing with learned probes (LatentGate);
- <10B base/tool/routing-agent architecture gains (Rethinking Scale);
- explicit evolving state for interacting/conflicting rules (Guidelines as Environments);
- deterministic authoritative-state executability gating before LLM selection and counterfactual replay (Don't Offer What Can't Be Done).

The surviving contribution remains the matched fixed-weight **authoritative semantic-result placement** study under the exact four-state contract and independent layer scoring, subject to independent re-review and all other W0 gates.