schema_version: 1.1.0
linear_issue: ENG-200
scope: TRAIN_DEV_ONLY
canonical_id_contract:
  policy_references: canonical_id_only
  qwen_outputs: handle_only
  visible_labels_are_treatment_factor: true
  canonical_ids_hidden_from_qwen: true
capabilities:
- canonical_id: db.resolve_claim
  handle: cap_01
  kind: procedure_id
  version: 1.0.0
  available: true
  side_effects: none
  provenance_required: true
  input_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/db.resolve_claim/input
  result_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/db.resolve_claim/result
  tool_budget:
    max_invocations: 1
    timeout_ms: 5000
    max_output_bytes: 65536
    max_rows: 128
    max_output_tokens: 0
  failure_semantics:
    fail_closed: true
    retry_policy: none
    codes:
    - INVALID_ARGUMENTS
    - TIMEOUT
    - UNAVAILABLE
    - CONTRACT_VIOLATION
  neutral_surface:
    label: Claim resolver
    description: Resolve one declared claim for a supplied scope and version.
  schema_adapted_dev_surface:
    label: Check a scoped claim
    description: Use when the request asks whether one declared claim holds for a particular scope and version.
- canonical_id: db.lookup_provenance
  handle: cap_02
  kind: view_id
  version: 1.0.0
  available: true
  side_effects: none
  provenance_required: true
  input_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/db.lookup_provenance/input
  result_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/db.lookup_provenance/result
  tool_budget:
    max_invocations: 1
    timeout_ms: 5000
    max_output_bytes: 131072
    max_rows: 512
    max_output_tokens: 0
  failure_semantics:
    fail_closed: true
    retry_policy: none
    codes:
    - INVALID_ARGUMENTS
    - TIMEOUT
    - UNAVAILABLE
    - CONTRACT_VIOLATION
  neutral_surface:
    label: Evidence lookup
    description: Retrieve source-linked records for a supplied source, scope and version.
  schema_adapted_dev_surface:
    label: Find supporting records
    description: Use when the request asks which source records are attached to an item in a scope and version.
- canonical_id: prolog.resolve_epistemic
  handle: cap_03
  kind: prolog_goal_id
  version: 1.0.0
  available: true
  side_effects: none
  provenance_required: true
  input_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/prolog.resolve_epistemic/input
  result_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/prolog.resolve_epistemic/result
  tool_budget:
    max_invocations: 1
    timeout_ms: 5000
    max_output_bytes: 65536
    max_rows: 0
    max_output_tokens: 0
  failure_semantics:
    fail_closed: true
    retry_policy: none
    codes:
    - INVALID_ARGUMENTS
    - TIMEOUT
    - UNAVAILABLE
    - NO_SOLUTION
    - CONTRACT_VIOLATION
  neutral_surface:
    label: Policy resolver
    description: Resolve a declared claim through the strict policy contract.
  schema_adapted_dev_surface:
    label: Resolve claim with policy rules
    description: Use when the request explicitly requires strict policy, conflict handling, or an auditable rule trace.
- canonical_id: python.threshold_check
  handle: cap_04
  kind: python_tool_id
  version: 1.0.0
  available: true
  side_effects: none
  provenance_required: false
  input_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/python.threshold_check/input
  result_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/python.threshold_check/result
  tool_budget:
    max_invocations: 1
    timeout_ms: 2000
    max_output_bytes: 16384
    max_rows: 0
    max_output_tokens: 0
  failure_semantics:
    fail_closed: true
    retry_policy: none
    codes:
    - INVALID_ARGUMENTS
    - TIMEOUT
    - UNAVAILABLE
    - CONTRACT_VIOLATION
  neutral_surface:
    label: Threshold check
    description: Compare a supplied numeric value with a supplied threshold.
  schema_adapted_dev_surface:
    label: Compare a value to a threshold
    description: Use for deterministic greater-than, less-than, or equal-to threshold checks.
- canonical_id: prompt.explain_result
  handle: cap_05
  kind: prompt_id
  version: 1.0.0
  available: true
  side_effects: none
  provenance_required: false
  input_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/prompt.explain_result/input
  result_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/prompt.explain_result/result
  tool_budget:
    max_invocations: 1
    timeout_ms: 10000
    max_output_bytes: 32768
    max_rows: 0
    max_output_tokens: 512
  failure_semantics:
    fail_closed: true
    retry_policy: none
    codes:
    - INVALID_ARGUMENTS
    - TIMEOUT
    - UNAVAILABLE
    - CONTRACT_VIOLATION
  neutral_surface:
    label: Result explainer
    description: Explain a structured result without changing it.
  schema_adapted_dev_surface:
    label: Explain an existing result
    description: Use only after a structured result already exists and the request asks for an explanation.
- canonical_id: clarification.missing_scope
  handle: cap_06
  kind: clarification_action
  version: 1.0.0
  available: true
  side_effects: none
  provenance_required: false
  input_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/clarification.missing_scope/input
  result_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/clarification.missing_scope/result
  tool_budget:
    max_invocations: 1
    timeout_ms: 1000
    max_output_bytes: 4096
    max_rows: 0
    max_output_tokens: 128
  failure_semantics:
    fail_closed: true
    retry_policy: none
    codes:
    - CONTRACT_VIOLATION
  neutral_surface:
    label: Ask for scope
    description: Request the missing scope required by the selected capability.
  schema_adapted_dev_surface:
    label: Ask which scope applies
    description: Use when the requested operation cannot be executed safely without a scope.
- canonical_id: clarification.missing_version
  handle: cap_07
  kind: clarification_action
  version: 1.0.0
  available: true
  side_effects: none
  provenance_required: false
  input_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/clarification.missing_version/input
  result_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/clarification.missing_version/result
  tool_budget:
    max_invocations: 1
    timeout_ms: 1000
    max_output_bytes: 4096
    max_rows: 0
    max_output_tokens: 128
  failure_semantics:
    fail_closed: true
    retry_policy: none
    codes:
    - CONTRACT_VIOLATION
  neutral_surface:
    label: Ask for version
    description: Request the missing version required by the selected capability.
  schema_adapted_dev_surface:
    label: Ask which version applies
    description: Use when the requested operation cannot be executed safely without a version.
- canonical_id: clarification.unsupported_goal
  handle: cap_08
  kind: clarification_action
  version: 1.0.0
  available: true
  side_effects: none
  provenance_required: false
  input_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/clarification.unsupported_goal/input
  result_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/clarification.unsupported_goal/result
  tool_budget:
    max_invocations: 1
    timeout_ms: 1000
    max_output_bytes: 4096
    max_rows: 0
    max_output_tokens: 128
  failure_semantics:
    fail_closed: true
    retry_policy: none
    codes:
    - CONTRACT_VIOLATION
  neutral_surface:
    label: Clarify request
    description: Ask for a supported goal when the request cannot be routed.
  schema_adapted_dev_surface:
    label: Clarify the requested operation
    description: Use when no declared capability matches the requested goal.
- canonical_id: reject.unsupported_write
  handle: cap_09
  kind: reject_action
  version: 1.0.0
  available: true
  side_effects: none
  provenance_required: false
  input_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/reject.unsupported_write/input
  result_schema_ref: CAPABILITY_IO_SCHEMAS.json#/capabilities/reject.unsupported_write/result
  tool_budget:
    max_invocations: 1
    timeout_ms: 1000
    max_output_bytes: 4096
    max_rows: 0
    max_output_tokens: 128
  failure_semantics:
    fail_closed: true
    retry_policy: none
    codes:
    - CONTRACT_VIOLATION
  neutral_surface:
    label: Decline modification
    description: Refuse state-changing operations in this read-only routing study.
  schema_adapted_dev_surface:
    label: Reject write or mutation request
    description: Use when the request would modify database, policy, files, or other protected state.
