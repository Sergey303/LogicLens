# Material replacement fixture

This synthetic file is the human-readable source for the first Epistemic Compiler Lab experiment.

## ASD100500

Material `ASD100500` replaces `ASD2` starting on 1 July 2026.

For hardware revision `A`, continue using `ASD2` after the transition date.

For hardware revision `B`, use `ASD100500` on and after the transition date.

Before 1 July 2026, both supported revisions use `ASD2`.

## Supported revisions

The fixture recognises only revisions `A` and `B`.

Any other revision must produce `unknown`, not an inferred default.

## Evidence policy

The Prolog layer may expose this section as evidence. The source remains authoritative if a generated fact or rule disagrees with it.